// Tuple is added by TS
// JS does not have tuple
// A Tuple is a fix lenght and fix typed type array
const grade: [string, number] = ["B", 7];

const person: {
  name: string;
  age: number;
  hobbies: string[];
  role: [number, string];
} = {
  name: "Onesmus",
  age: 21,
  hobbies: ["sports", "cooking"],
  role: [2, "author"],
};
